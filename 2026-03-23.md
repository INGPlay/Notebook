---
type: 일지
date: 2026-03-23 17:14:33
created: 2026-03-23 09:17:45
categories:
- 인포마인드 / 업무 - 인포마인드
---
# 2026-03-23

<br>

- INSTACK 개발
    - DB 반영
    - 공통 개발
        - Logging - AOP
    - API 개발
        - CMS
            - 유저 관리 API

<br>

<br>

```java
● # 목록 조회
  curl -X GET "http://localhost:8080/api/cms/user/list?page=1&size=10" \
    -H "Authorization: Bearer {accessToken}"

  # 목록 조회 (userSe 필터)
  curl -X GET "http://localhost:8080/api/cms/user/list?page=1&size=10&userSe=A" \
    -H "Authorization: Bearer {accessToken}"

  # 단건 조회 - 관리자
  curl -X GET "http://localhost:8080/api/cms/user/A/admin01" \
    -H "Authorization: Bearer {accessToken}"

  # 단건 조회 - 업무사용자
  curl -X GET "http://localhost:8080/api/cms/user/E/emp01" \
    -H "Authorization: Bearer {accessToken}"

  # 단건 조회 - 일반사용자
  curl -X GET "http://localhost:8080/api/cms/user/G/user01" \
    -H "Authorization: Bearer {accessToken}"

  # 생성 - 관리자
  curl -X POST "http://localhost:8080/api/cms/user/A" \
    -H "Authorization: Bearer {accessToken}" \
    -H "Content-Type: application/json" \
    -d '{
      "userId": "admin01",
      "pwd": "password123!",
      "userNm": "홍길동",
      "eml": "admin@example.com",
      "mtelno": "01012345678",
      "gndrSe": "M",
      "telno": "0212345678",
      "addr": "서울시 강남구",
      "daddr": "101호"
    }'

  # 생성 - 업무사용자
  curl -X POST "http://localhost:8080/api/cms/user/E" \
    -H "Authorization: Bearer {accessToken}" \
    -H "Content-Type: application/json" \
    -d '{
      "userId": "emp01",
      "pwd": "password123!",
      "userNm": "김철수",
      "eml": "emp@example.com",
      "mtelno": "01087654321",
      "gndrSe": "M",
      "telno": "0287654321",
      "addr": "서울시 서초구",
      "daddr": "202호"
    }'

  # 생성 - 일반사용자
  curl -X POST "http://localhost:8080/api/cms/user/G" \
    -H "Authorization: Bearer {accessToken}" \
    -H "Content-Type: application/json" \
    -d '{
      "userId": "user01",
      "pwd": "password123!",
      "userNm": "이영희",
      "eml": "user@example.com",
      "mtelno": "01011112222",
      "gndrSe": "F",
      "telno": "0311112222",
      "addr": "경기도 수원시",
      "daddr": "303호",
      "zip": "16001"
    }'

  # 수정 - 관리자
  curl -X PUT "http://localhost:8080/api/cms/user/A/admin01" \
    -H "Authorization: Bearer {accessToken}" \
    -H "Content-Type: application/json" \
    -d '{
      "userNm": "홍길동(수정)",
      "eml": "admin_new@example.com",
      "mtelno": "01099998888",
      "gndrSe": "M",
      "telno": "0212345678",
      "addr": "서울시 마포구",
      "daddr": "404호"
    }'

  # 수정 - 비밀번호 포함
  curl -X PUT "http://localhost:8080/api/cms/user/A/admin01" \
    -H "Authorization: Bearer {accessToken}" \
    -H "Content-Type: application/json" \
    -d '{
      "userNm": "홍길동",
      "eml": "admin@example.com",
      "mtelno": "01012345678",
      "gndrSe": "M",
      "pwd": "newpassword456!"
    }'

  # 삭제 - 관리자
  curl -X DELETE "http://localhost:8080/api/cms/user/A/admin01" \
    -H "Authorization: Bearer {accessToken}"

  # 삭제 - 업무사용자
  curl -X DELETE "http://localhost:8080/api/cms/user/E/emp01" \
    -H "Authorization: Bearer {accessToken}"

  # 삭제 - 일반사용자
  curl -X DELETE "http://localhost:8080/api/cms/user/G/user01" \
    -H "Authorization: Bearer {accessToken}"


```

<br>
