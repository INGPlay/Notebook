---
date: 2025-01-24 09:50:50
created: 2025-01-22 11:11:12
categories:
- 인포마인드 - JejuNU / Dreamy - ExBuilder
---

## 리포트 함수 정리

<br>

- 현장실습 - 선발결과
    - 지원신청서 출력

```
function printPrgrmReport()
{
	let printTitle = "지원신청서";
	let filePath = "A/AFE/AFE02";
	let fileName = "AFE2120205_R01";
	let reportParameters	= PatisReport.defaultReportParamters(app);
	let parameters			= PatisReport.defaultParamters(app);

		PatisReport.addReportParameter(reportParameters
			, "TARGET_ID"
			, g_STUNO
			, "학번"
			, app.lookup("ds_grid01").getValue(0, "STUNM"));
		PatisReport.addReportParameter(reportParameters
			, "OGNZ_CLSF_SE"
			, app.lookup("ds_grid01").getValue(0, "OGNZ_CLSF_SE")
			, "조직분류구분"
			, app.lookup("ds_grid01").getValue(0, "OGNZ_CLSF_SE_NM"));
		PatisReport.addReportParameter(reportParameters
			, "CPL_CD"
			, app.lookup("ds_grid01").getValue(0, "CPL_CD")
			, "이수코드"
			, app.lookup("ds_grid01").getValue(0, "CPL_NM"));
		PatisReport.addReportParameter(reportParameters
			, "SBJCT_CD"
			, app.lookup("ds_grid01").getValue(0, "SBJCT_CD")
			, "교과목코드"
			, app.lookup("ds_grid01").getValue(0, "SBJCT_NM"));
		PatisReport.addReportParameter(reportParameters
			, "COLL_REQU_SN"
			, g_COLL_REQU_SN
			, "모집요건순번"
			, "모집요건순번");
		PatisReport.addReportParameter(reportParameters
			, "PRGRM_SN"
			, g_PRGRM_SN
			, "프로그램순번"
			, g_PRGRM_SN);
		PatisReport.addReportParameter(reportParameters
			, "ENT_ID"
			, g_ENT_ID
			, "기업ID"
			, "기업ID");
			
	PatisReport.PopupPrint(app, printTitle, filePath, fileName, reportParameters, parameters);
}
```

<br>

<br>

- 현장실습 - 현장실습 협약서

```
function printPrgrmReport()
{
	let rowIndex = app.lookup("DG_GRID01").getSelectedRowIndex();
	
	let printTitle = "협약서";
	let filePath = "A/AFE/AFE03";
	let fileName = "AFE2120301_R01";
	let reportParameters	= PatisReport.defaultReportParamters(app);
	let parameters			= PatisReport.defaultParamters(app);

		PatisReport.addReportParameter(reportParameters
			, "ENT_ID"
			, app.lookup("ds_grid01").getValue(rowIndex, "ENT_ID")
			, "기업ID"
			, app.lookup("ds_grid01").getValue(rowIndex, "ENT_NM_KORN"));
			
		PatisReport.addReportParameter(reportParameters
			, "PRGRM_SN"
			, app.lookup("ds_grid01").getValue(rowIndex, "PRGRM_SN")
			, "프로그램순번"
			, app.lookup("ds_grid01").getValue(rowIndex, "PRGRM_NM"));
			
		PatisReport.addReportParameter(reportParameters
			, "TARGET_ID"
			, app.lookup("ds_grid01").getValue(rowIndex, "STUNO")
			, "학번"
			, app.lookup("ds_grid01").getValue(rowIndex, "STUNM"));
			
			
	PatisReport.PopupPrint(app, printTitle, filePath, fileName, reportParameters, parameters);
}
```

<br>

<br>

- 현장실습 - 수료증 출력

```
function printPrgrmReport()
{
	let rowIndex = app.lookup("DG_GRID01").getSelectedRowIndex();
	
	let printTitle = "수료증";
	let filePath = "A/AFE/AFE04";
	let fileName = "AFE2120401_R01";
	let reportParameters	= PatisReport.defaultReportParamters(app);
	let parameters			= PatisReport.defaultParamters(app);

		PatisReport.addReportParameter(reportParameters
			, "TARGET_ID"
			, app.lookup("ds_grid01").getValue(rowIndex, "TARGET_ID")
			, "학번"
			, app.lookup("ds_grid01").getValue(rowIndex, "TARGET_ID"));
			
		PatisReport.addReportParameter(reportParameters
			, "PRGRM_SN"
			, app.lookup("ds_grid01").getValue(rowIndex, "PRGRM_SN")
			, "프로그램순번"
			, app.lookup("ds_grid01").getValue(rowIndex, "PRGRM_SN"));
			
		PatisReport.addReportParameter(reportParameters
			, "ENT_ID"
			, app.lookup("ds_grid01").getValue(rowIndex, "ENT_ID")
			, "기업ID"
			, app.lookup("ds_grid01").getValue(rowIndex, "ENT_ID"));
			
		PatisReport.addReportParameter(reportParameters
			, "COLL_REQU_SN"
			, app.lookup("ds_grid01").getValue(rowIndex, "COLL_REQU_SN")
			, "모집요건순번"
			, app.lookup("ds_grid01").getValue(rowIndex, "COLL_REQU_SN"));
			
		PatisReport.addReportParameter(reportParameters
			, "FNSHD_ISSU_SN"
			, app.lookup("ds_result").getValue(rowIndex, "FNSHD_ISSU_SN")
			, "발급번호순번"
			, app.lookup("ds_result").getValue(rowIndex, "FNSHD_ISSU_SN"));
			
	PatisReport.PopupPrint(app, printTitle, filePath, fileName, reportParameters, parameters);
}
```

<br>

<br>

- 봉사활동관리 - 봉사활동확인서 출력

```
function Form_printClick (obj, e)
{
	if(PatisUtils.isNull(app.lookup("ds_grid01").getValue(0, "TARGET_ID")))
	{
		alert("조회된 학생이 없습니다.");
		return false;
	}
	else
	{
		let printTitle 			= "봉사활동확인서";
		let filePath 			= "A/AHS/AHS03";
		let fileName 			= "AHS2140303_R01";
		let reportParameters	= PatisReport.defaultReportParamters(app);
		let parameters			= PatisReport.defaultParamters(app);
	
			PatisReport.addReportParameter(reportParameters, "TARGET_ID", app.lookup("ds_grid01").getValue(0, "TARGET_ID"), "학번", app.lookup("ds_grid01").getValue(0, "TARGET_ID"));
			//PatisReport.addReportParameter(reportParameters, "PRIVACY_YN", PatisCommonButton.getPrivacyValue(), "개인정보열람여부", PatisCommonButton.getPrivacyValue());
			PatisReport.addReportParameter(reportParameters, "PRIVACY_YN", '1', "개인정보열람여부", '1');
		
		PatisReport.PopupPrint(app, printTitle, filePath, fileName, reportParameters, parameters);
	}
}
```

<br>

- 현장실습 - 출석부 조회
    - 출석부 출력

```
function Form_printClick()
{
	if(app.lookup("ds_grid01").getRowCount() == 0)
	{
		alert("출력할 데이터가 존재하지 않습니다.");
		return false;
	}
	
	let printTitle = "출석부";
	let filePath = "A/AFE/AFE06";
	let fileName = "AFE2120603_R01";
	let reportParameters	= PatisReport.defaultReportParamters(app);
	let parameters			= PatisReport.defaultParamters(app);
		
		PatisReport.addReportParameter(reportParameters
			, "PRGRM_SN"
			, app.lookup("ds_grid01").getValue(0, "PRGRM_SN")
			, "프로그램 순번"
			, app.lookup("ds_grid01").getValue(0, "PRGRM_SN"));
			
		PatisReport.addReportParameter(reportParameters
			, "ENT_IDS"
			, "('" + app.lookup("ds_grid01").getValue(0, "ENT_ID") + "')"
			, "기업ID"
			, app.lookup("ds_grid01").getValue(0, "ENT_ID"));
			
		PatisReport.addReportParameter(reportParameters
			, "TARGET_IDS"
			, "('" + app.lookup("ds_grid01").getValue(0, "TARGET_ID") + "')"
			, "학번"
			, app.lookup("ds_grid01").getValue(0, "TARGET_ID"));
			
	PatisReport.PopupPrint(app, printTitle, filePath, fileName, reportParameters, parameters);
}
```

<br>

<br>

- 현장실습 - 주간보고서 등록
    - 주간보고서 출력

```
function Form_printClick()
{
	let selectRowIndexList = app.lookup("DG_GRID01").getCheckRowIndices();
	
	if(selectRowIndexList.length == 0)
	{
		alert("대상을 선택해주세요.");
		return false;
	}
	
	let printTitle = "주간보고서";
	let filePath = "A/AFE/AFE06";
	let fileName = "AFE2120604_R01";
	let reportParameters	= PatisReport.defaultReportParamters(app);
	let parameters			= PatisReport.defaultParamters(app);
	let targetIds = "";
		
	for(var index = 0; index < selectRowIndexList.length; index++)
	{
		targetIds += "'" + app.lookup("ds_grid01").getValue(selectRowIndexList[index], "STUNO") + "',";
	}
	
	targetIds = targetIds.substring(0, targetIds.length - 1);
	targetIds = "(" + targetIds + ")";
	
		PatisReport.addReportParameter(reportParameters
			, "PRGRM_SN"
			, app.lookup("ds_grid01").getValue(0, "PRGRM_SN")
			, "프로그램 순번"
			, app.lookup("ds_grid01").getValue(0, "PRGRM_SN"));
			
		PatisReport.addReportParameter(reportParameters
			, "ENT_ID"
			, app.lookup("ds_grid01").getValue(0, "ENT_ID")
			, "기업ID"
			, app.lookup("ds_grid01").getValue(0, "ENT_ID"));
		
		PatisReport.addReportParameter(reportParameters
			, "TARGET_ID"
			, targetIds
			, "학번"
			, "학번");
			
	PatisReport.PopupPrint(app, printTitle, filePath, fileName, reportParameters, parameters);
}
```

<br>

- 현장실습 - 중간점검서등록
    - 중간점검서 출력

```
function Form_printClick()
{
	if(app.lookup("ds_mid_chckd").getRowCount() == 0)
	{
		return false;
	}
	
	if(app.lookup("ds_mid_chckd").getRowState(0) == cpr.data.tabledata.RowState.INSERTED)
	{
		alert("저장 후 이용해주세요.");
		return false;
	}
	
	let printTitle = "중간점검서";
	let filePath = "A/AFE/AFE03";
	let fileName = "AFE2120304_R01";
	let reportParameters	= PatisReport.defaultReportParamters(app);
	let parameters			= PatisReport.defaultParamters(app);
	let targetIds = "";
		
		PatisReport.addReportParameter(reportParameters
			, "PRGRM_SN"
			, app.lookup("ds_mid_chckd").getValue(0, "PRGRM_SN")
			, "프로그램 순번"
			, app.lookup("ds_mid_chckd").getValue(0, "PRGRM_SN"));
			
		PatisReport.addReportParameter(reportParameters
			, "ENT_ID"
			, app.lookup("ds_mid_chckd").getValue(0, "ENT_ID")
			, "기업ID"
			, app.lookup("ds_mid_chckd").getValue(0, "ENT_ID"));
		
		PatisReport.addReportParameter(reportParameters
			, "TARGET_ID"
			, app.lookup("ds_mid_chckd").getValue(0, "STUNO")
			, "학번"
			, app.lookup("ds_mid_chckd").getValue(0, "STUNO"));
			
		PatisReport.addReportParameter(reportParameters
			, "COLL_REQU_SN"
			, app.lookup("ds_mid_chckd").getValue(0, "COLL_REQU_SN")
			, "모집요건 순번"
			, app.lookup("ds_mid_chckd").getValue(0, "COLL_REQU_SN"));
			
	PatisReport.PopupPrint(app, printTitle, filePath, fileName, reportParameters, parameters);
}
```

<br>

<br>

- 현장실습 - 최종보고서등록
    - 최종보고서 출력

```
function Form_printClick()
{
	let selectRowIndexList = app.lookup("DG_GRID01").getCheckRowIndices();
	
	if(selectRowIndexList.length == 0)
	{
		alert("대상을 선택해주세요.");
		return false;
	}
	
	let printTitle = "최종보고서";
	let filePath = "A/AFE/AFE06";
	let fileName = "AFE2120605_R01";
	let reportParameters	= PatisReport.defaultReportParamters(app);
	let parameters			= PatisReport.defaultParamters(app);
	let targetIds = "";
		
	for(var index = 0; index < selectRowIndexList.length; index++)
	{
		targetIds += "'" + app.lookup("ds_grid01").getValue(selectRowIndexList[index], "STUNO") + "',";
	}
	
	targetIds = targetIds.substring(0, targetIds.length - 1);
	targetIds = "(" + targetIds + ")";
	
		PatisReport.addReportParameter(reportParameters
			, "PRGRM_SN"
			, app.lookup("ds_grid01").getValue(0, "PRGRM_SN")
			, "프로그램 순번"
			, app.lookup("ds_grid01").getValue(0, "PRGRM_SN"));
			
		PatisReport.addReportParameter(reportParameters
			, "ENT_ID"
			, app.lookup("ds_grid01").getValue(0, "ENT_ID")
			, "기업ID"
			, app.lookup("ds_grid01").getValue(0, "ENT_ID"));
		
		PatisReport.addReportParameter(reportParameters
			, "TARGET_ID"
			, targetIds
			, "학번"
			, "학번");
			
	PatisReport.PopupPrint(app, printTitle, filePath, fileName, reportParameters, parameters);
}
```

<br>

<br>

- 현장실습 - 이력서 관리
    - 자소서 출력

```
function printPrgrmReport(e)
{
	let control = e.control;
	let selfIntroSn = 0;
	
	switch(control.id){
		case "BTN_PRINT01" :
			selfIntroSn = '1';			
			break;
		case "BTN_PRINT02" :
			selfIntroSn = '2';			
			break;
		case "BTN_PRINT03" :
			selfIntroSn = '3';			
			break;
		
	}
	
	let rowIndex = app.lookup("DG_GRID01").getSelectedRowIndex();
	
	let printTitle = "이력서";
	let filePath = "A/AFE/AFE05";
	let fileName = "AFE2120502_R01";
	let reportParameters	= PatisReport.defaultReportParamters(app);
	let parameters			= PatisReport.defaultParamters(app);

		PatisReport.addReportParameter(reportParameters
			, "SELF_INTRO_SN"
			, selfIntroSn
			, "자소서순번"
			, selfIntroSn);
			
		PatisReport.addReportParameter(reportParameters
			, "TARGET_ID"
			, app.lookup("ds_grid01").getValue(rowIndex, "STUNO")
			, "학번"
			, app.lookup("ds_grid01").getValue(rowIndex, "STUNM"));
			
	PatisReport.PopupPrint(app, printTitle, filePath, fileName, reportParameters, parameters);
}
```

<br>

<br>

- 현장실습기관확인
    - 서약서 출력

```
function printRghtReport()
{
	let printTitle = "현장실습생 권리 보호 이행 서약서";
	let filePath = "A/AFE/AFE05";
	let fileName = "AFE2120501_R01";
	let reportParameters	= PatisReport.defaultReportParamters(app);
	let parameters			= PatisReport.defaultParamters(app);
	
		PatisReport.addReportParameter(reportParameters
			, "ENT_ID"
			, app.lookup("ds_ent_prgrm_aply").getValue(0, "ENT_ID")
			, "기업ID"
			, app.lookup("ds_ent_prgrm_aply").getValue(0, "ENT_NM_KORN"));
			
		PatisReport.addReportParameter(reportParameters
			, "PRGRM_SN"
			, app.lookup("ds_prgrm").getValue(0, "PRGRM_SN")
			, "프로그램순번"
			, app.lookup("ds_prgrm").getValue(0, "PRGRM_NM"));
			
	PatisReport.PopupPrint(app, printTitle, filePath, fileName, reportParameters, parameters);
}
```

<br>

<br>

- 학생생활관(아라) - 고지서 출력

```
openReportPopup('DDO4010501_R02');

function openReportPopup(fileName){
	
	let selectedRow = app.lookup("ds_detail").getRow(0);
	let printTitle 			= "학생생활관 - 납부고지서 출력";
	//let filePath 			= "D/DDO/DDO08";
	let filePath 			= "D/DDO/DDO05";
	let reportParameters	= PatisReport.defaultReportParamters(app);
	let parameters			= PatisReport.defaultParamters(app);

	 parameters["P_ROLE_CHECK"] = "0"; // 권한 체크 여부
	 parameters["P_ROLE_SEARCH"] = "1"; // 권한 미체크시 조회 권한 부여 여부 (출력) [출력 버튼만 사용가능]
	 parameters["P_ROLE_SAVE"] = "0"; // 권한 미체크시 저장 권한 부여 여부 (다른 파일로 저장) [PDF, 엑셀, PPT등 사용]

	let CAMP_SE  = selectedRow.getValue("CAMP_SE");
	let COLL_YR  = selectedRow.getValue("COLL_YR");
	let COLL_SE  = selectedRow.getValue("COLL_SE");
	let COLL_SN  = selectedRow.getValue("COLL_SN");
	let BLDG_CD  = selectedRow.getValue("BLDG_ALTMNT_CD");
	let DORMR_NO = selectedRow.getValue("DORMR_NO");
	let IPAY_CYNO = selectedRow.getValue("IPAY_CYNO");

 	PatisReport.addReportParameter(reportParameters, "CAMP_SE",CAMP_SE, "캠퍼스 구분","CAMP_SE");
 	PatisReport.addReportParameter(reportParameters, "COLL_YR",COLL_YR, "모집 년도","COLL_YR");
 	PatisReport.addReportParameter(reportParameters, "COLL_SE",COLL_SE, "모집 구분","COLL_SE");
 	PatisReport.addReportParameter(reportParameters, "BLDG_CD",BLDG_CD, "건물명","BLDG_CD");
 	PatisReport.addReportParameter(reportParameters, "COLL_SN",COLL_SN, "모집명","COLL_SN");
 	PatisReport.addReportParameter(reportParameters, "DORMR_NO",DORMR_NO, "입주학생번호","DORMR_NO");
 	PatisReport.addReportParameter(reportParameters, "IPAY_CYNO",IPAY_CYNO, "차수","IPAY_CYNO");
	
	console.log(`${CAMP_SE}, ${COLL_YR}, ${COLL_SE}, ${COLL_SN},${BLDG_CD},${DORMR_NO},${IPAY_CYNO}`)
	
	PatisReport.PopupPrint(app, printTitle, filePath, fileName, reportParameters, parameters);

}
```

<br>

<br>

<br>

- 학생생활관(아라) - 기혼자납부고지서 출력

```
openReportPopup("DDO4010809_R01");

function openReportPopup(fileName){
	
	let selectedRow = app.lookup("ds_detail").getRow(0);
	let printTitle 			= "학생생활관 - 기혼자 납부고지서 출력";
	let filePath 			= "D/DDO/DDO08";
	let reportParameters	= PatisReport.defaultReportParamters(app);
	let parameters			= PatisReport.defaultParamters(app);

	 parameters["P_ROLE_CHECK"] = "0"; // 권한 체크 여부
	 parameters["P_ROLE_SEARCH"] = "1"; // 권한 미체크시 조회 권한 부여 여부 (출력) [출력 버튼만 사용가능]
	 parameters["P_ROLE_SAVE"] = "0"; // 권한 미체크시 저장 권한 부여 여부 (다른 파일로 저장) [PDF, 엑셀, PPT등 사용]

	let YR            = selectedRow.getValue("YR");
	let ROOM_CD       = selectedRow.getValue("ROOM_CD");
	let MARRIED_NO    = selectedRow.getValue("MARRIED_NO");
	let BLDG_CD       = selectedRow.getValue("BLDG_CD");
	let MNGCO_GIVE_MM = selectedRow.getValue("MNGCO_GIVE_MM");

 	PatisReport.addReportParameter(reportParameters, "YR",YR, "년도","YR");
 	PatisReport.addReportParameter(reportParameters, "ROOM_CD",ROOM_CD, "호실","ROOM_CD");
 	PatisReport.addReportParameter(reportParameters, "MARRIED_NO",MARRIED_NO, "기혼자","MARRIED_NO");
 	PatisReport.addReportParameter(reportParameters, "BLDG_CD",BLDG_CD, "건물","BLDG_CD");
 	PatisReport.addReportParameter(reportParameters, "MNGCO_GIVE_MM",MNGCO_GIVE_MM, "지급월","MNGCO_GIVE_MM");
	
	console.log(`${YR}, ${ROOM_CD}, ${MARRIED_NO}, ${BLDG_CD},${MNGCO_GIVE_MM}`)
	
	PatisReport.PopupPrint(app, printTitle, filePath, fileName, reportParameters, parameters);

}
```

<br>