---
date: 2025-06-12 16:24:18
created: 2025-06-12 13:42:04
categories:
- 인포마인드 - JejuNU / Portal - 관리자
- 인포마인드 - JejuNU / 인터뷰 - JNU
---

## \[정리\] 2025-06-12 인터뷰 - 포탈 모바일

<br>

- 내용
    - SSO
        - agentProc.jsp - 유비즈 에서 준 코드 그대로
        - 직원과 학생의 세션 시간이 다르다(직원의 시간이 더 길다 : 학생 90분, 직원 240분)
    - 아이폰 화면 버그 (SSO 관련)
        - 링크 가기 전에 SSO 로그인 처리 (아이디, 패스워드 던지고)
    - 암호화 정책
        - DB에 솔트키 가지고 있고, 백단에서 복호화하기
    - 자동로그인
        - 링크 가기 전에 SSO 로그인 처리예정 (아이디, 패스워드 던지고)
    - Z학번
        - 박소정 팀장님한테 받음
    - 포탈에 접근이 안되는 롤코드
        - tis에 바로 가는 롤코드는 DB 함수에 바로 추가해야 함
        - F\_TIS\_REDIRECT() 함수 내에 추가
    - 2차 인증
        - 권한관리 - 그룹관리에서 2차인증 필요한 그룹 설정
    - 포틀릿
    - 개발서버 통합, 포탈 유저 차이
        - 뷰에서 특정 조건이 안 맞으면 포탈로 데이터가 안 넘어옴
        - DREAMY.POT\_EA\_USER\_V
    - 사이트 관리
        - 아이피 넣고 하면 메뉴가 안 뜸
        - 스크립트 에러가 남 → 아트피큐가 해결
    - 안드로이드 사이트 URL - 반복되는 정적변수를 전역변수로 바꿔주기
    - 캐시
        - Velocity 캐시 1시간마다 체크
        - env.xml 
            - env.xml 파일은 svn과 운영서버의 내용이 다름
            - velocity cache 설정할 수 있음
        - DB 캐시 xml에서 <cache> 태그 검색
    - 푸시서버
        - 올릴 때 VM 옵션 필요 - 받아야 함
        - jnu-keys 필요
        - ssh 붙어서 jnu-pusher-1.2.0.jar 로 배포
    - 모바일 (아이폰 / 안드로이드)
        - 배포키, 사인키 받기 - 장봉수 선생님
        - 주석된 코드는 사용되지 않음
    - 넥서스

<br>

- 코드 참고

```java
## portal\user\PortalUserServiceImpl

// ...

&& (login.getAsLogin() !=null || (login.getAsLogin() == null && portalUser.getChgYn().equals("N"))) //비밀번호 변경대상이 아닌경우 실서버 체크 / 관리자 로그인일때는 비체크

// ...
```

    - 비밀번호 변경대상 체크

<br>

```java
## portal\login\ok.htm

#if ( $pswdInfo.authYn == 'Y' )
	parent.fn_setCertiReg();    ## 2차 인증
#else
```

    - 2차 인증 함수 호출 구문

<br>

```java
## config\cache\ehcache.xml

<cache name="cacheJnuBoard" maxElementsInMemory="2048" eternal="false" timeToIdleSeconds="600" timeToLiveSeconds="600" overflowToDisk="false" diskPersistent="false" statistics="true" memoryStoreEvictionPolicy="LRU" />
<cache name="cacheJnuMeal" maxElementsInMemory="2048" eternal="false" timeToIdleSeconds="3600" timeToLiveSeconds="3600" overflowToDisk="false" diskPersistent="false" statistics="true" memoryStoreEvictionPolicy="LRU" />
<cache name="cachePortletApi" maxElementsInMemory="2048" eternal="false" timeToIdleSeconds="3600" timeToLiveSeconds="3600" overflowToDisk="false" diskPersistent="false" statistics="true" memoryStoreEvictionPolicy="LRU" />
```

    - 게시판 캐시 설정하는 3줄

<br>

```java
## env-server.xml

<!-- View Cache -->
<entry key="view.loader.velocity.cache">false</entry>
<entry key="view.resolver.excel.cache">false</entry>
<entry key="view.resolver.velocity.cache">false</entry>
<entry key="view.resolver.jstl.cache">false</entry>
```

    - Velocity 캐시 설정 - 운영서버 반영 시 바로 반영 안되는 이유
    - 현재 운영서버에는 다 true로 되어 있음

<br>

```java
## \config\spring\context-view.xml

<prop key="resource.loader.file.modification_check_interval">3600</prop>
```

    - Velocity 변경 체크 간격 설정
    - 현재 1시간마다 파일 변경 체크